import { StyleSheet } from 'react-native';

export const stadiumHeroStyles = StyleSheet.create({
  image: {
    aspectRatio: 4 / 3,
    maxHeight: 384,
  },
  action: {
    padding: 16,
  },
  iconContainer: {
    backgroundColor: 'rgb(255 255 255 / 0.1)',
    borderColor: 'rgb(255 255 255 / 0.1)',
  },
});
